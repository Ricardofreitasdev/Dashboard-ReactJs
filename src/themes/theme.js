import { createTheme } from "@material-ui/core";

const theme = createTheme({
  palette: {
    primary: {
      main: "#E31C58",
    },
    secondary: {
      main: "#142032",
    },
  },
});


export default theme;