import React from "react";
import useStyles from "./style";

export function App() {
 

  const styles = useStyles();
  const user = { name: "Ricardo", age: 29, user: "admin" };

  return (
    <div>
      home
    </div>
  );
}

